import styled from 'styled-components/native';

export const StyledAuth = styled.View`
  padding: 57px 38px;
`;

export const StyledPage = styled.View``;

const ContainerBase = styled.View`
  padding: 15px;
  gap: 25px;
`;

export const ContainerHome = styled(ContainerBase)``;

export const ContainerNutricional = styled(ContainerBase)`
  padding: 15px;
  gap: 25px;
`;

export const ContainerDaily = styled(ContainerBase)`
  gap: 36px;
`;

export const ContainerProfile = styled(ContainerBase)`
  gap: 57px;

  position: relative !important;
`;

export const ContainerResults = styled(ContainerBase)``
